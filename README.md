How to use this files in your vim ? 
===================================

```shell
    shell> ln /path/to/your/dotfiles/_vim  ~/.vim
    shell> ln /path/to/your/dotfiles/_vimrc  ~/.vimrc
```

Quit from your vim , and restart it.

